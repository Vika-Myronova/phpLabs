{% extends 'base.html.twig' %}

{% block title %}Author index{% endblock %}

{% block body %}
    <h1>Author index</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.birth_year) }}
    <button class="btn btn-primary">Фільтрувати</button>
    {{ form_end(form) }}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Birth_year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for author in authors %}
            <tr>
                <td>{{ author.id }}</td>
                <td>{{ author.name }}</td>
                <td>{{ author.birthYear }}</td>
                <td>
                    <a href="{{ path('app_author_show', {'id': author.id}) }}">show</a>
                    <a href="{{ path('app_author_edit', {'id': author.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_author_new') }}">Create new</a>

    <div class="pagination">
        {% if currentPage > 1 %}
            <a href="{{ path('app_author_index', { page: currentPage - 1, itemsPerPage: itemsPerPage }) }}">Previous</a>
        {% endif %}
        <span>Page {{ currentPage }} of {{ totalPages }}</span>
        {% if currentPage < totalPages %}
            <a href="{{ path('app_author_index', { page: currentPage + 1, itemsPerPage: itemsPerPage }) }}">Next</a>
        {% endif %}
    </div>

    <form method="get">
        <label for="itemsPerPage">Items per page:</label>
        <select name="itemsPerPage" onchange="this.form.submit()">
            <option value="2" {% if itemsPerPage == 2 %}selected{% endif %}>2</option>
            <option value="5" {% if itemsPerPage == 5 %}selected{% endif %}>5</option>
            <option value="10" {% if itemsPerPage == 10 %}selected{% endif %}>10</option>
        </select>
    </form>
{% endblock %}
